#include "main.h"
#include <stdio.h>

/**
 * _strlen - gives us the length of the string
 *
 * @s : var pointed to where string is stored.
 */
int _strlen(char *s)
{
	int length = strlen(*s);

	_putchar(length + 0);

	return (0);
}
