#include "main.h"
#include <stdio.h>

/**
 * mul - multiplying two int's
 * @a : integer one
 * @b : integer two
 * Return: prints out result.
 */

int mul(int a, int b)
{
	result = (a * b)

	return (result)
}
