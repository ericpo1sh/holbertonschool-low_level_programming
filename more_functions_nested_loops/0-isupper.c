#include "main.h"
#include <stdio.h>
/**
 * _isupper - checks if character is uppercase
 *
 * @c : character to be checked
 * Return: 0 if is lower, 1 if upper
 */

int _isupper(int c)
{
	if (c == 99)
{
	return (0);
}
	else
	return (1);
